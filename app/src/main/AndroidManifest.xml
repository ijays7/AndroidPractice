<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ijays.apolo">

    <!-- 网络权限-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!--外部存储读权限，调试工具加载本地补丁需要-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar.Immerse">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.NestedActivity"
            android:theme="@style/AppTheme.NoActionBar.Immerse" />
        <activity
            android:name=".activity.WaveActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity.ActiveProgressbarActivity" />
        <activity android:name=".activity.ViewStubTestActivity" />
        <activity android:name=".activity.BehaviorTestActivity" />
        <activity android:name=".activity.MaterialAnimActivity" />
        <activity android:name=".activity.SharedElementActivity" />
        <activity android:name=".activity.TransitionActivity" />
    </application>

</manifest>